# Noura — Landing

Web estática de un solo archivo (`index.html`). Sin build, sin dependencias.

## Deploy
- Vercel: importar este repo como proyecto, framework "Other". Cada push a `main` redespliega automáticamente.

## TODO antes de publicar
- [ ] Sustituir los productos, ingredientes y precios de ejemplo por datos verificados de envases reales de Mercadona. Están en dos sitios de `index.html`:
      1. El bloque `demoData` dentro de `<script>` (demo del hero).
      2. La tarjeta `.menu-demo` de la sección Premium (menú, lista y precios).
- [ ] Poner las URLs reales de Instagram y TikTok en el footer (ahora apuntan a `#`).
- [ ] Cuando haya dominio propio: añadirlo en Vercel → Settings → Domains.
